title: "Engineering Updates"
description: "Public release notes, operational updates, and incident summaries."
theme: minima
timezone: America/New_York
collections:
  updates:
    output: true
    permalink: /updates/:name/
defaults:
  - scope:
      path: ""
      type: updates
    values:
      layout: update
      permalink: /updates/:name/
